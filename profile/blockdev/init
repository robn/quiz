# sourced by init2

QUIZ_DEV_N=${QUIZ_DEV_N:-0}

for d in /dev/disk/by-id/virtio-blockdev* ; do
  q=/dev/quiz$QUIZ_DEV_N
  QUIZ_DEV_N=$(($QUIZ_DEV_N + 1))

  ln -sf $d $q
  echo "quiz: blockdev: linked $q -> $d" >> /dev/kmsg
done

# vim: ft=bash
