# sourced by init2

# clean out /etc/zfs from previous runs
rm -rf /etc/zfs
mkdir /etc/zfs

# rebuild module dependencies; since the overlay "added" two new modules
depmod

# load modules
modprobe zfs

# vim: ft=bash
